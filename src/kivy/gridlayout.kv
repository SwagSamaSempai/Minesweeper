<GridLayout>:
    padding: [app.window.size[0] / 30, app.window.size[1] / 30]
    col_default_width: 30
    row_default_height: 30
    cols: int((app.window.size[0] - root.padding[0] * 2) / root.col_default_width)
    rows: int((app.window.size[1] - root.padding[1] * 2) / root.row_default_height)